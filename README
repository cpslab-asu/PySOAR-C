poetry installation from: https://python-poetry.org/docs/master/

after installing:
1) create the virtual environment
poetry install

2) cd demos

3) run the NLF_example.py in demo folder and store the results
poetry run python NLF_example.py

