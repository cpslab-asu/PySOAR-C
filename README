poetry installation from: https://python-poetry.org/docs/master/

after installing:
1) go into arch2022_benchmarks
cd arch_benchmarks

2) create the virtual environment
poetry update

3) Install matlab in environemnt path

3) run the AT1 demo and store the results in Test_AT1 folder
poetry run python benchmarks AT1 -f"Test_AT1"

